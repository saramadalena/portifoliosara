import fs from 'node:fs'

const file = new URL('../src/App.tsx', import.meta.url)
let s = fs.readFileSync(file, 'utf8')

const marker = `      {/* ── CONTATO ── */}\n      <section id="contato"`

if (!s.includes('id="reconhecimento"') && s.includes(marker)) {
  const recognition = `      {/* ── RECONHECIMENTO PROFISSIONAL ── */}\n      <section id="reconhecimento" style={{ backgroundColor:T.white,borderTop:\`1px solid \${T.rule}\`,padding: isMobile?'48px 24px':isWide?'80px 80px':'72px 48px' }}>\n        <div style={{ maxWidth:'1200px',margin:'0 auto' }}>\n          <div style={{ marginBottom:isMobile?'32px':'44px' }}>\n            <p style={{ fontSize:'10px',fontWeight:700,letterSpacing:'0.22em',textTransform:'uppercase',color:T.magenta,margin:'0 0 12px' }}>Reconhecimento profissional</p>\n            <h2 style={{ fontFamily:'Playfair Display, serif',fontSize:'clamp(30px,4vw,48px)',lineHeight:1.08,letterSpacing:'-0.03em',margin:'0 0 16px',color:T.ink }}>O trabalho também é feito de relações</h2>\n            <p style={{ fontSize:'14px',fontWeight:300,lineHeight:1.8,color:T.inkMid,margin:0,maxWidth:'720px' }}>Parte da minha atuação acontece justamente na relação com outras pessoas e áreas. Por isso, a percepção de profissionais que trabalharam comigo também ajuda a contar essa trajetória.</p>\n          </div>\n\n          <div style={{ display:'grid',gridTemplateColumns:isMobile?'1fr':'repeat(3,minmax(0,1fr))',gap:'1px',backgroundColor:T.rule,border:\`1px solid \${T.rule}\` }}>\n            {[\n              { nome:'Felipe Ramos', cargo:'Coordenação de Comunicação e Marketing · Colégio Arnaldo', texto:'Destacou minha capacidade de manter o discurso da área alinhado, atuar de forma prática no dia a dia e contribuir para o trabalho em equipe.' },\n              { nome:'Mário Lonczinski', cargo:'CEO · SPDATA', texto:'Percebeu uma mudança concreta na comunicação da empresa, que ganhou mais vida e passou a despertar maior interesse do público interno.' },\n              { nome:'Leila Vasconcelos', cargo:'Analista de Comunicação e Marketing Sênior', texto:'Ressaltou minha abertura para ouvir feedbacks, refletir sobre eles e transformar rapidamente os aprendizados em ajustes na prática.' },\n            ].map((item, i) => (\n              <article key={item.nome} style={{ backgroundColor:T.white,padding:isMobile?'28px 24px':'32px 28px',minHeight:isMobile?'auto':'250px',display:'flex',flexDirection:'column',justifyContent:'space-between',gap:'28px' }}>\n                <p style={{ fontFamily:'Playfair Display, serif',fontSize:'20px',fontWeight:400,lineHeight:1.5,color:T.ink,margin:0 }}>{item.texto}</p>\n                <div style={{ paddingTop:'18px',borderTop:\`1px solid \${T.ruleLight}\` }}>\n                  <p style={{ fontSize:'11px',fontWeight:700,letterSpacing:'0.08em',textTransform:'uppercase',color:i % 2 === 0 ? T.navy : T.magenta,margin:'0 0 6px' }}>{item.nome}</p>\n                  <p style={{ fontSize:'11px',fontWeight:300,lineHeight:1.5,color:T.inkLight,margin:0 }}>{item.cargo}</p>\n                </div>\n              </article>\n            ))}\n          </div>\n        </div>\n      </section>\n\n`
  s = s.replace(marker, recognition + marker)
}

fs.writeFileSync(file, s)
console.log('Reconhecimento profissional inserido antes do contato.')
