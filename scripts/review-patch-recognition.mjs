import fs from 'node:fs'

const file = new URL('../src/App.tsx', import.meta.url)
let s = fs.readFileSync(file, 'utf8')

// O reconhecimento entra antes de Sobre para funcionar como validação da trajetória,
// e não como um bloco tardio imediatamente antes do contato.
const marker = `      {/* ── SOBRE ── */}\n      <section id="sobre"`

if (!s.includes('id="reconhecimento"') && s.includes(marker)) {
  const recognition = `      {/* ── RECONHECIMENTO PROFISSIONAL ── */}\n      <section id="reconhecimento" style={{ backgroundColor:T.white,borderTop:\`1px solid \${T.rule}\`,padding: isMobile?'44px 24px':isWide?'68px 80px':'60px 48px' }}>\n        <div style={{ maxWidth:'1200px',margin:'0 auto' }}>\n          <div style={{ marginBottom:isMobile?'28px':'36px' }}>\n            <p style={{ fontSize:'10px',fontWeight:700,letterSpacing:'0.22em',textTransform:'uppercase',color:T.magenta,margin:'0 0 10px' }}>Reconhecimento profissional</p>\n            <h2 style={{ fontFamily:'Playfair Display, serif',fontSize:'clamp(26px,3.4vw,42px)',lineHeight:1.08,letterSpacing:'-0.03em',margin:'0 0 14px',color:T.ink }}>O trabalho também é feito de relações</h2>\n            <p style={{ fontSize:'13px',fontWeight:300,lineHeight:1.75,color:T.inkMid,margin:0,maxWidth:'680px' }}>A percepção de profissionais que trabalharam comigo ajuda a mostrar como essa atuação acontece na prática, especialmente na relação com pessoas, áreas e lideranças.</p>\n          </div>\n\n          <div style={{ display:'grid',gridTemplateColumns:isMobile?'1fr':'repeat(3,minmax(0,1fr))',gap:'12px' }}>\n            {[\n              { nome:'Felipe Ramos', cargo:'Coordenação de Comunicação e Marketing · Colégio Arnaldo', texto:'Destacou minha capacidade de manter o discurso da área alinhado, atuar de forma prática no dia a dia e contribuir para o trabalho em equipe.' },\n              { nome:'Mário Lonczinski', cargo:'CEO · SPDATA', texto:'Percebeu uma mudança concreta na comunicação da empresa, que ganhou mais vida e passou a despertar maior interesse do público interno.' },\n              { nome:'Leila Vasconcelos', cargo:'Analista de Comunicação e Marketing Sênior', texto:'Ressaltou minha abertura para ouvir feedbacks, refletir sobre eles e transformar rapidamente os aprendizados em ajustes na prática.' },\n            ].map((item, i) => (\n              <article key={item.nome} style={{ backgroundColor:T.bg,padding:isMobile?'24px 20px':'26px 24px',display:'flex',flexDirection:'column',justifyContent:'space-between',gap:'22px',borderTop:\`2px solid \${i % 2 === 0 ? T.navy : T.magenta}\` }}>\n                <p style={{ fontFamily:'Playfair Display, serif',fontSize:'17px',fontWeight:400,lineHeight:1.48,color:T.ink,margin:0 }}>{item.texto}</p>\n                <div style={{ paddingTop:'14px',borderTop:\`1px solid \${T.ruleLight}\` }}>\n                  <p style={{ fontSize:'10px',fontWeight:700,letterSpacing:'0.08em',textTransform:'uppercase',color:i % 2 === 0 ? T.navy : T.magenta,margin:'0 0 5px' }}>{item.nome}</p>\n                  <p style={{ fontSize:'10px',fontWeight:300,lineHeight:1.45,color:T.inkLight,margin:0 }}>{item.cargo}</p>\n                </div>\n              </article>\n            ))}\n          </div>\n        </div>\n      </section>\n\n`
  s = s.replace(marker, recognition + marker)
}

fs.writeFileSync(file, s)
console.log('Reconhecimento profissional reposicionado antes de Sobre e simplificado visualmente.')
